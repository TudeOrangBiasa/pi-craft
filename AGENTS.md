# AGENTS.md

TypeScript + Bun. See `CODING_STANDARDS.md` (toolchain, lint, monadic style, extension rules) and `CONTEXT.md` (domain vocabulary).

## Agent skills

### Issue tracker

Issues live as local markdown under `.scratch/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five canonical labels, strings equal names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
